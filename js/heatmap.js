// Replace "YOUR_GITHUB_USERNAME" with your actual GitHub username
const gitHubUsername = "aukik";

// Call the GitHubCalendar function and pass the required options
GitHubCalendar(".calendar", gitHubUsername, {
  responsive: true,
  global_stats: false,
  tooltips: true,
});
